# Copiar este archivo como .env y ajustar los valores segun el entorno.
# En Vite, solo las variables con prefijo VITE_ llegan al codigo del navegador.

# URL base de la API del backend Spring Boot
VITE_API_URL=http://localhost:8080/api
