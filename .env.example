# Copiar este archivo como .env y poner una contrasena propia.
# Docker Compose lee automaticamente el archivo .env de este directorio.
#
# Se usa para dos cosas a la vez:
#  - la contrasena de root con la que se crea el MySQL del contenedor
#  - la que el backend usa para conectarse a esa misma base

DB_PASSWORD=
